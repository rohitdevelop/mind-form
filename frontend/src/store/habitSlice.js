// Placeholder for habit slice
export const initialHabitState = { habits: [] };
export default function habitReducer(state = initialHabitState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
