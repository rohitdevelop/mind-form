// Placeholder for auth slice
export const initialAuthState = { user: null };
export default function authReducer(state = initialAuthState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
